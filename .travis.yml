language: dart
dart:
  - dev

branches:
  only: 
    - master
    - release

jobs:
  include:
    - stage: Build
      script: bash ./tool/build.sh
    - stage: Deploy
      script: bash ./tool/publish.sh

stages:
  - Build
  - Deploy
    if: branch = release

cache:
  directories:
    - $HOME/.pub-cache